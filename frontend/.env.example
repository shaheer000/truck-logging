# Copy to .env.local for local dev (or set in Vercel project settings for prod).
# Leave blank in dev — Vite proxies /api to localhost:8000 via vite.config.js.
# In Vercel, set this to your EC2 backend URL with the /api suffix.
VITE_API_BASE_URL=https://api.example.com/api
